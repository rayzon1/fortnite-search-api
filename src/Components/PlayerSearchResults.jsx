import React from 'react';

export default function PlayerSearchResults({ userResults, userId }) {
    
    console.log(userResults.length);
    console.log(userId);
    
    return (
        <>
        </>
    )
}